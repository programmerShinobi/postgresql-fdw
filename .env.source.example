# =============================================================================
# Source database to import FROM (used by scripts/migration/*).
# Copy to `.env.source` (gitignored) and fill in. NEVER commit the real file.
#   cp .env.source.example .env.source
# =============================================================================
SRC_HOST=
SRC_PORT=15409
SRC_USER=
SRC_DB=
SRC_PASSWORD=
